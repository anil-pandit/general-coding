package binary_search_questions.find_floor_of_an_element;public class Main {    //floor : greatest element which is smaller than given number    //ceil: smallest element which is greater than given number    private static int findFloor(int[] arr, int element) {        int start = 0, end = arr.length-1;        int res = -1;        while(start <= end) {            int mid = start + (end - start) / 2;            if (element == arr[mid])                return mid;            else if (element > arr[mid]){                res = mid;                start = mid + 1;            }            else                end = mid - 1;        }        return res;    }    public static void main(String[] args) {        int arr[] = {2, 5, 8, 12, 45, 67, 78};        System.out.println(findFloor(arr, 77));    }}