package array_questions.buy_sell_stock_single_day;import static java.lang.Math.max;import static java.lang.Math.min;public class Main {    //find the min element first and find the profit from min element and then from that profit find the max profit    private static int buySellStock(int[] stock) {        int min_element = Integer.MAX_VALUE;        int net_profit = Integer.MIN_VALUE;        int n = stock.length;        for(int i = 0; i < n; i++){            min_element = min(min_element, stock[i]);            int profit = stock[i] - min_element;            net_profit = max(net_profit, profit);        }        return net_profit;    }    public static void main(String[] args) {        int stock[] = {7,1,5,3,6,4};        System.out.println(buySellStock(stock));    }}